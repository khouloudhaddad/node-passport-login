<style>
    :root {
        --bg-body: #eef2f7;
        --color-primary: #727cf5;
        --color-primary-hover: #6973e3;
        --default-text-color: #6c757d;
        --secondary: #6c757d;
        --success: #0acf97;
        --info: #39afd1;
        --warning: #ffc35a;
        --danger: #fa5c7c;
        --danger-btn-hover-bg: #e75573;
    }

    a {
        text-decoration: none;
        color: #727cf5;
    }

    ul {
        list-style: none;
        margin: 0;
    }

    body {
        background-color: var(--bg-body);
        font-size: 1rem;
        color: var(--default-text-color);
    }

    .btn {
        padding: 0.45rem 0.9rem;
    }

    .btn-primary {
        background-color: var(--color-primary) !important;
        border-color: var(--color-primary) !important;
    }

    .btn-primary:hover,
    .btn-primary:focus {
        background-color: var(--color-primary-hover) !important;
        border-color: var(--color-primary-hover) !important;
    }

    .btn-danger {
        background-color: var(--danger) !important;
        border-color: var(--danger) !important;
    }

    .btn-danger:hover,
    .btn-danger:focus {
        background-color: var(--danger-btn-hover-bg) !important;
        border-color: var(--danger-btn-hover-bg) !important;
    }

    header nav {
        background-color: #313a46;
    }

    .navbar-brand {
        color: #fff;
        font-size: 0.95rem;
    }

    .navbar-brand:hover,
    .navbar-brand:focus {
        color: #eef2f7;
    }

    .btn-actions {
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .btn-actions .btn {
        display: flex;
        align-items: center;
    }

    .btn-actions .btn svg {
        margin-right: 0.25rem;
    }

    .text-primary {
        color: var(--color-primary) !important;
    }

    .card {
        box-shadow: 0px 0px 35px 0px rgba(154, 161, 171, 0.15) !important;
        border-color: transparent !important;
        padding: 1rem;

    }

    .card-header{
        background-color: transparent !important;
        border-bottom: none !important;
    }

    footer {
        background-color: #fff;
        padding: 30px 25px;
        position: fixed;
        bottom: 0;
        width: 100%;
        left: 0;
    }

    .page-title-wrapper {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
        align-items: center;

    }

    .page-title-wrapper h4 {
        font-size: 18px;
        margin: 0;
        line-height: 75px;
        overflow: hidden;
        white-space: nowrap;
        text-overflow: ellipsis;
    }

    .page-title-wrapper .breadcrumb {
        padding-top: 8px;
    }

    .m-0 {
        margin: 0 !important;
    }

    .breadcrumb-item+.breadcrumb-item::before {
        font-size: 24px;
        font-family: 'Material Icons';
        font-weight: normal;
        font-style: normal;
        line-height: 1.3;
    }

    .breadcrumb-item+.breadcrumb-item::before {
        float: left;
        padding-right: 0.5rem;
        color: rgba(108, 117, 125, 0.75);
        content: "\e5cc";
    }
</style>